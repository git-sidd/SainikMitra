/** @type {import('next').NextConfig} */
const nextConfig = {
   
        images: {
          domains: ['bharatkeveer.gov.in'], // Add your image domain here
        },
    
};

export default nextConfig;
